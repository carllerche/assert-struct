# Skills listing

This directory contains skills for teaching agents how to use assert-struct.

If you wish to use these skills, you can copy them into the appropriate directory for your agent (e.g., `.agents/skills`), but you can also use the [Symposium](https://symposium.dev) tool which will install them automatically into any crate that depends on `assert-struct`.
